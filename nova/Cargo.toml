[package]
name = "nova"
version = "1.0.0"
edition = "2024"

[dependencies]
nova-core = { path = "../nova-core" }
nova-2d = { path = "../nova-2d", optional = true}

[features]
default = ["2d"]
2d = ["dep:nova-2d"]
egui = ["nova-core/egui"]